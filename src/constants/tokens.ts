/**
 * 스탬핏 디자인 토큰
 * 모든 컴포넌트는 이 파일의 값만 참조한다.
 * 직접 hex 값을 사용하지 않는다.
 */

// ─────────────────────────────────────────────
// 색상
// ─────────────────────────────────────────────
export const colors = {
  // Primary — 인디고
  // 사용처: 주요 버튼, 활성 탭, FocusCard 배경(1곳), 포커스 링
  primary: {
    50:  '#EEF2FF',
    100: '#E0E7FF',
    200: '#C7D2FE',
    400: '#818CF8',
    500: '#6366F1',
    600: '#4F46E5', // ★ 메인 — 버튼, 활성 상태
    700: '#4338CA', // hover
  },

  // Accent — amber
  // 사용처: 달성 혜택, 추천 뱃지, 특별 이벤트 칩 "만" 사용
  // 날짜 경과·안내 배너에는 사용 금지 → red / gray 사용
  accent: {
    50:  '#FFFBEB',
    100: '#FEF3C7',
    200: '#FDE68A',
    400: '#FBBF24',
    500: '#F59E0B', // ★ 메인
    700: '#B45309',
    800: '#92400E',
  },

  // Success — green
  // 사용처: 확정 완료, 절약 금액, 성공 토스트
  success: {
    50:  '#ECFDF5',
    100: '#D1FAE5',
    500: '#10B981', // ★ 메인
    700: '#047857',
    900: '#065F46',
  },

  // Danger — red
  // 사용처: 날짜 경과(3일+), 위험 액션, 에러
  danger: {
    50:  '#FEF2F2',
    100: '#FEE2E2',
    500: '#EF4444', // ★ 메인
    700: '#B91C1C',
    900: '#991B1B',
  },

  // Neutral — gray
  gray: {
    50:  '#F9FAFB', // 앱 전체 배경
    100: '#F3F4F6', // 비활성 배경, 구분선
    200: '#E5E7EB', // 경계선
    300: '#D1D5DB', // 비활성 요소
    400: '#9CA3AF', // placeholder, 부가 텍스트
    500: '#6B7280', // 보조 텍스트
    700: '#374151', // 본문
    900: '#111827', // 제목
  },

  // 흰색
  white: '#FFFFFF',
} as const;

// ─────────────────────────────────────────────
// 타이포그래피
// ─────────────────────────────────────────────
export const typography = {
  size: {
    xs:    '11px', // 뱃지, 태그, 마이크로 텍스트
    sm:    '12px', // 레이블, 캡션, 안내 문구
    base:  '14px', // 본문, 리스트 항목
    md:    '15px', // 카드 제목, 중요 본문
    lg:    '16px', // 입력 필드, 일반 버튼
    xl:    '17px', // 주요 CTA 버튼
    '2xl': '20px', // 섹션 숫자
    '3xl': '24px', // 슬라이드 타이틀
    '4xl': '28px', // 히어로 숫자 (요약 카드)
    '5xl': '32px', // 대형 숫자
  },
  weight: {
    regular:   400,
    medium:    500,
    semibold:  600,
    bold:      700,
    extrabold: 800,
  },
  lineHeight: {
    tight:   1.2,
    snug:    1.35,
    normal:  1.5,
    relaxed: 1.6,
    loose:   1.75,
  },
} as const;

// ─────────────────────────────────────────────
// 모서리 반경
// ─────────────────────────────────────────────
export const radius = {
  sm:    '8px',    // 작은 칩, 뱃지 내부
  md:    '12px',   // 입력 필드, 작은 카드
  lg:    '16px',   // 일반 카드 (Surface Card)
  xl:    '20px',   // 큰 카드, 오늘의 일정 카드
  '2xl': '24px',   // 바텀시트 상단
  '3xl': '28px',   // 온보딩 일러스트 카드
  full:  '9999px', // pill 버튼, 원형 버튼, 칩
} as const;

// ─────────────────────────────────────────────
// 간격
// ─────────────────────────────────────────────
export const spacing = {
  screenX:      '16px', // 화면 좌우 패딩
  cardPad:      '16px', // 카드 내부 패딩
  cardPadSm:    '12px', // 작은 카드 내부 패딩
  cardGap:       '8px', // 카드 사이 간격
  sectionGap:   '24px', // 섹션 사이 간격
  sectionLabel: '20px', // 섹션 레이블 위 여백
} as const;

// ─────────────────────────────────────────────
// 터치 영역 (모바일 최소 기준)
// ─────────────────────────────────────────────
export const touch = {
  min:    '44px', // 모든 탭 가능 요소 최소값
  button: '48px', // 일반 버튼
  cta:    '54px', // 주요 CTA 버튼
  nav:    '64px', // BottomNav
} as const;

// ─────────────────────────────────────────────
// 그림자
// ─────────────────────────────────────────────
export const shadow = {
  sm:      '0 1px 3px rgba(0,0,0,0.06)',
  md:      '0 2px 8px rgba(0,0,0,0.08)',
  lg:      '0 4px 16px rgba(0,0,0,0.10)',
  primary: '0 4px 12px rgba(79,70,229,0.25)', // 인디고 포인트
  accent:  '0 4px 12px rgba(245,158,11,0.25)', // amber 포인트
} as const;

// ─────────────────────────────────────────────
// 경계선
// ─────────────────────────────────────────────
export const border = {
  default: `1px solid ${colors.gray[200]}`,
  thick:   `1.5px solid ${colors.gray[200]}`,
  focus:   `1.5px solid ${colors.primary[600]}`,
  error:   `1.5px solid ${colors.danger[500]}`,
  dashed:  `1.5px dashed ${colors.gray[300]}`,
  accent:  `1.5px solid ${colors.primary[600]}`,
} as const;

// ─────────────────────────────────────────────
// 색상 사용 원칙 (주석)
// ─────────────────────────────────────────────
/**
 * 인디고(primary.600):
 *   ✓ 주요 버튼 (확정하기, 저장, 시작하기)
 *   ✓ 활성 탭, 활성 상태 표시
 *   ✓ FocusCard 배경 (현황 탭 1개만)
 *   ✓ 포커스 링
 *   ✗ 일반 카드 배경 금지 — 오늘의 일정 카드도 흰색 사용
 *
 * amber(accent.500):
 *   ✓ 달성 혜택 행 배경
 *   ✓ 추천 뱃지 (⭐ 추천)
 *   ✓ 특별 이벤트 칩 (싸인회, 하이터치회 등)
 *   ✗ 날짜 경과 표시 금지 → danger.500 사용
 *   ✗ 재관람 안내 배너 금지 → accent.50 배경 + accent.800 텍스트
 *
 * red(danger.500):
 *   ✓ 날짜 경과 3일+ 표시
 *   ✓ 위험 액션 (삭제, 초기화) 텍스트
 *   ✓ 에러 메시지, 에러 border
 *
 * green(success.500):
 *   ✓ 확정 완료 상태 border-left
 *   ✓ 절약 금액 텍스트
 *   ✓ 성공 토스트
 */
